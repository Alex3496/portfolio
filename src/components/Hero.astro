---
interface Props {
	title: string;
	tagline?: string[];
	align?: "start" | "center";
}

const { align = "center", tagline, title } = Astro.props;
---

<div class:list={["hero stack gap-4", align]}>
	<div class="stack gap-2">
		<h1 class="title gradient-text">{title}</h1>
		<div class="tagline-container">
			{
				Array.isArray(tagline)
					? tagline.map((line) => <p class="tagline">{line}</p>)
					: null
			}
		</div>
	</div>
	<slot />
</div>

<style>
	.hero {
		font-size: var(--text-lg);
		text-align: center;
		padding-top: 5rem;
	}

	.title,
	.tagline {
		max-width: 37ch;
		margin-inline: auto;
	}

	.title {
		font-size: var(--text-3xl);
		line-height: 1.1;
		font-weight: 800;
		margin-bottom: 1rem;
	}

	.tagline {
		color: var(--gray-400);
		line-height: 1.5;
	}

	@media (min-width: 50em) {
		.hero {
			font-size: var(--text-xl);
		}

		.start {
			text-align: start;
		}

		.start .title,
		.start .tagline {
			margin-inline: unset;
		}

		.title {
			font-size: var(--text-5xl);
		}
	}
</style>
